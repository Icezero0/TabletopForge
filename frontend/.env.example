# 本地开发（推荐）：不设置 VITE_API_ORIGIN，请求走 Vite 代理（同源，无跨域）
# 需先启动后端：cd backend && ./run_backend.sh
#
# VITE_API_PREFIX=/api/v1

# 仅在前端要直连后端地址时使用（会触发 CORS，需后端允许你的前端 origin）
# VITE_API_ORIGIN=http://localhost:8000
# VITE_API_ORIGIN=http://127.0.0.1:8000
