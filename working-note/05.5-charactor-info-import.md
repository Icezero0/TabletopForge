添加快速导入角色信息功能，直接输入一个大文本框，交给大语言模型提取结构化字段，在界面中表单显示，编辑确认后写入数据库。

使用：.env
Qwen3.7-Max 
sk-aa6f4695a13e409393d1c5cc92f79ea7 
client = OpenAI(
    # 若没有配置环境变量，请用阿里云百炼API Key将下行替换为：api_key="sk-xxx",
    # 各地域的API Key不同。获取API Key：https://help.aliyun.com/zh/model-studio/get-api-key
    api_key=os.getenv("DASHSCOPE_API_KEY"),
    # 以下为华北2（北京）地域的URL，各地域的URL不同。
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)
适配openai api，允许.env 配置其他模型、base url、apikey


其他：
1. 角色颜色系统，每个角色可以选择自己的主色，用于默认的绘制、pointer、高亮边框。
2. 更好的地图资源管理交互体验，和角色一样，每个已导入的地图可以有预览、名称，然后用类似的popover控制进场。
3. 角色卡界面选择取消选择豁免、技能熟练项后，应自动扣除加上的熟练项。
4. 从房间进入角色卡编辑详情页，返回应返回房间。